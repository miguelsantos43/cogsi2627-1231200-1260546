/**
 * Configuration package for the Bookstore application.
 * 
 * <p>This package contains Spring configuration classes for initializing and configuring 
 * the application:</p>
 * 
 * <ul>
 *   <li>{@link com.example.bookstore.config.DataInitializer} - Initializes sample data on application startup</li>
 * </ul>
 * 
 * <p>Configuration classes use Spring's {@code @Configuration} annotation to define beans 
 * and application setup logic.</p>
 */
package com.example.bookstore.config;
